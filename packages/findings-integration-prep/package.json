{
  "name": "@databridge/findings-integration-prep",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Shared 'integration-prep report' generator. Given a source-system extract + a CRM extract, identifies records that need create / update / reject before sync. Used by Salesforce and Dynamics 365 adapters.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src --ext .ts",
    "test": "vitest run --passWithNoTests",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@databridge/adapter-spec": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "vitest": "^1.5.0"
  }
}
